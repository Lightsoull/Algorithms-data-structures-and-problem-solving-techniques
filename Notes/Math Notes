Sigma which mean summation is distributed linearly over equation  Z=X*Y      
so      Sigma Z =  ( Sigma X )*( Sigma Y )  and you can put sigma with a condition and  distribute it normally
------------------------------------------------------------------------------------------------------------------------------------------
if the angle exceeds 90 degree the cosine of angle turns to -ve value 
if the angle exceeds 180 degree the sine of angle turns to -ve value 
-------------------------------------------------------------------------------------------------------------------------------------------
Note if you want to do combination from a given set of N elements the combination set starting from 1 element up to N elements will be as 
if you are doing bitmasking from 1 to (1<<N)  in time 2^N
------------------------------------------------------------------------------------------------------------------------------------------
The symmetric of a number is if i have an array of n elements and i have a value V then its symetric value is n-V    
Properties of the symmetric 
1- any number plus its symetric equals to n 
------------------------------------------------------------------------------------------------------------------------------------------
zero power zero is math error
-----------------------------------------------------------------------------------------------------------------------------------------
First degree equation is the strightline equation
-----------------------------------------------------------------------------------------------------------------------------------------
There is a theorm states that for any even number greater than 2 can be expressed as sum of two primes .
------------------------------------------------------------------------------------------------------------------------------------------
the distance or the difference between any number and the first prime after it or the last prime before it is very small
------------------------------------------------------------------------------------------------------------------------------------------
Note if you have the decimal of this binary 00101 and i want to update decimal for this binary 00111 so just make 00010 and add it to 
the decimal of the 00101 binary 
----------------------------------------------------------------------------------------------------------------------------------------
If i want b-a  is equal to a-(b+1)+1
----------------------------------------------------------------------------------------------------------------------------------------
To maximize the product of a set of numbers increase the smaller first and if you want to decrease the product decrease the smaller 
number first
-----------------------------------------------------------------------------------------------------------------------------------------
How to Know if the following 3 sides can form a triangle so there is the answer   check if the smaller two sides if their sum is greater 
than the largest side so it can form a triangle else NO
----------------------------------------------------------------------------------------------------------------------------------------
Any circle with 3 sides forming a triangle inside it , it must be a right angled triangle 
----------------------------------------------------------------------------------------------------------------------------------------
By linearity of expectation you can think of the equation of the result without expectation then distribute the expectation
----------------------------------------------------------------------------------------------------------------------------------------
Way to calculate the probability is to know the  ( the number of choices that lead to the answer / the total number of choices )
----------------------------------------------------------------------------------------------------------------------------------------
If you are comparing between two numbers and you want to reduce the power so multiply both sides to log
----------------------------------------------------------------------------------------------------------------------------------------
If i have two numbers x and y and i am wondering x^y or y^x gives larger value there is nothing can tell, i will need to calculate both
values and know
----------------------------------------------------------------------------------------------------------------------------------------
About probability if you have a set contining diifferent families and you want the probability of choosing a pair of persons one from
familiy A and the other from familiy B 
Wrong answer is to consider that i will choose one from A then one from B OR  i will choose one from B then one from A bec i don't said
that 
The true answer is probability of having 2 persons from those two families are   
(number of pairs of two families) / (total number of pairs)  =  ( number of members of A * number of members of B ) / (  total number 
of pairs )
----------------------------------------------------------------------------------------------------------------------------------------
Geometric series : sigma from i=0 to inf   r^i  = 1/(1-r)
---------------------------------------------------------------------------------------------------------------------------------------
if    X / Y   and Y>X   if you add the amount M to X and to Y the result will be larger   ( X+M ) / (Y+M)
proof
X / Y  -   ( X+M ) / (Y+M)  =( Y(X+M) - X(Y+M) ) / ( Y(Y+M) )  = (MY-XM) / (Y^2+YM)       
so if Y>X  it will increase but else it will decrease
----------------------------------------------------------------------------------------------------------------------------------------
If you want to know the bigger value just subtract and see whether the difference is +ve or -ve
----------------------------------------------------------------------------------------------------------------------------------------
To know whether a four points make a square not nessasary parallel to co-ordinates or not check that   
1- the four sides are equal
2- get the distances of the two diagonals and check that they are equal and equal to that distance by bythagorth 
( to make sure that it has 90 degree angle )
----------------------------------------------------------------------------------------------------------------------------------------
When you hit a ball to the floor there is a normal which is at the middle of the floor a line makes 90 degree with both 
sides of the floor the angle of incidence with the normal equals to the angle of reflection with the normal
link --------->   http://scienceworld.wolfram.com/physics/AngleofReflection.html
----------------------------------------------------------------------------------------------------------------------------------------
nPr   P is permuation the n is the amount of numbers and r is the size of the set
----------------------------------------------------------------------------------------------------------------------------------------
Math Note if i have a Average= (x+y)/2 to add another value z so minmize the Average it must be less than the (x+y)/2 ( previous average)
(x+y+z)/3.

proof 
-----
(x+y+((x+y)/2))/3 = x/3+y/3+x/6+y/6 = x/2+y/2 so if z is less than (x+y)/2 so it will be less than x/2+y/2
----------------------------------------------------------------------------------------------------------------------------------------
The Labeled Tree is a tree where every node in it have a path to a certer node let it be node one for example in different ways the 
number of the ways is n^(n-2).
link  http://mathworld.wolfram.com/LabeledTree.html
----------------------------------------------------------------------------------------------------------------------------------------
Little Fermand theorem
if n is prime then a^n % n = a % n
----------------------------------------------------------------------------------------------------------------------------------------
i=N
Sigma i^2  = (N*(N+1)*(2*N+1))/6
i=0
----------------------------------------------------------------------------------------------------------------------------------------
The square root function decreases in a big rate 
eg if i have x and sqrt(x) then sqrt( sqrt(x) ) and so on it decreases from 10^18 to 1 in few steps and when it reaches one it turns to 
be contant.
----------------------------------------------------------------------------------------------------------------------------------------
1-If X is unknown and i know X%Ci for some integers Ci for every 0<i<n then i can know  X % lcm(all Ci).
2- Some mod properties
  -a1=a2 (mod n) and b1=b2 (mod n), a1+b1=a2+b2 (mod n) and a1*b1 = a2*b2 (mod n) ans (a1-a2) is divisable by n
3- Some Lcm and gcd properties
  -gcd( P.N,P.M ) = P*gcd( N,M )  & lcm( P.N,P.M ) = P*lcm( N,M ).
  
  - If gcd( N,M ) = 1, then
  gcd( N*M , Q) = gcd( N,Q ) * gcd( M,Q )
  lcm( N*M,Q ) =( lcm(N,Q) * lcm(M,Q) ) / Q
  
  -gcd(gcd(N, M), P) = gcd(N, gcd(M, P))
   lcm(lcm(N, M), P) = lcm(N, lcm(M, P)).
  
 -gcd(M, N, P) = gcd(gcd(M, N),P) = gcd(M, gcd(N, P))
  lcm(M, N, P) = lcm(lcm(M, N),P) = lcm(M, lcm(N, P)).
  
  For integers N1, ..., Nk, k ≥ 2,
lcm(gcd(N1, M), gcd(N2, M), ..., gcd(Nk, M)) = gcd(lcm(N1, ..., Nk), M)
gcd(lcm(N1, M), lcm(N2, M), ..., lcm(Nk, M)) = lcm(gcd(N1, ..., Nk), M).

Lcm is the union of the prime factors and gcd is the intersection of the prime factors.
----------------------------------------------------------------------------------------------------------------------------------------
Permutations and Combinations  n is the numbers and r is the parts want to fill

permutations with replacement(repition)
n^r
permutations without replacement 
nPr
combinations with replacement 
( n-1+r )! / ( r! * (n-1)! )
combination without replacemant 
nCr = n!/((n-r)!*r!)
----------------------------------------------------------------------------------------------------------------------------------------
Note if you have some numbers from 1 to n and you want to put a number in every of three packets but in sorted order so
the number of ways to to do so is------> combinations with replacement ( n-1+r )! / ( r! * (n-1)! ).
----------------------------------------------------------------------------------------------------------------------------------------
Sigma i=0 to i=K   i * (n choose i ) = K * 2^(K-1)
----------------------------------------------------------------------------------------------------------------------------------------
Any triangle with integer vertexes it is doubled area is integer.
----------------------------------------------------------------------------------------------------------------------------------------
If A * B = C * D  this doesn't mean that A is equal to C or D and B is equal to C or D, this means that A*B and C*D have the same
prime factors but not the same numbers.
----------------------------------------------------------------------------------------------------------------------------------------
If you are dealing with equations and you want to write (num)/2 and num is not even and we want to get the ceiled value so 
its written way is (num+1)/2.
----------------------------------------------------------------------------------------------------------------------------------------
iff A>B then A%B <= (A+1)/2 .
----------------------------------------------------------------------------------------------------------------------------------------
If a number let it be T decreases by T%=B every time and T>B so if T is 10^18 it will be zero in log(n) complexity
----------------------------------------------------------------------------------------------------------------------------------------
Force Value Technique
If i have a equation where the variable is x , ax-diff=b and i want to get the value of x that makes the diff min possible so force (let 
the value of diff equal to zero ) the value of diff to be min possible which is zero then get the x if it supposed to be integer and it 
is float then there is no integer value for x to make diff equals to zero to to be min diff yu may ceil the value of x .
----------------------------------------------------------------------------------------------------------------------------------------
-a/b + b/a will be minminum when a=b and as much as the difference increase the value will increase.
- a/b and c/d if the difference between them equal that doesn't mean their actual values are equal.
----------------------------------------------------------------------------------------------------------------------------------------
Factorization equation
(-b +/- sqrt( b^2-4ac ) )/ (2a) 
eg ax^2+bx+c=0  note take the c with its sign.
----------------------------------------------------------------------------------------------------------------------------------------
If i have a circle let it be circle1 it is center (x1,y1) and radius r1 and i want to get the center of the circle that contain circle1 
let it be circle2 it is center (x2,y2) and radius r2 there is a range of points where the center of circle2 can exist given that the 
center of circle2 is on the x-axis so the leftside of the range is x2-delta and the rightside is x2+delta on x-axis where delta is 
 double delta= sqrt(((r2-r1)*(r2-r1))-(y2*y2)); 
----------------------------------------------------------------------------------------------------------------------------------------
if I sum up the diff between ranges for eg range(x,y) sum+= (y-x) so i am taking from x to y-1 ---> x-(y-1)+1.
so if i update a range with value take care i am calculating all the range y-x+1 or the diff between it y-x.
----------------------------------------------------------------------------------------------------------------------------------------
If you have a Regular Polygon (all sides & angles equal and you want to take a regular polygon from it so the number of sides of the 
new polygon must be factor of the number of points of the orginal polygon.
----------------------------------------------------------------------------------------------------------------------------------------
If i have an array of integers of size up to 10^5 the number of distinct elements that their number of occurance in the 
array equals to their value  is at most 500 bec (n*(n+1))/2 <= 10^5.
----------------------------------------------------------------------------------------------------------------------------------------
Sum of arithematic progression sequence ( sequence where the difference between values equal eg 2,4,6 diffrence=2 )
sum= ((a1+an)*n)/2 where a1 is the value of the first element and an is the value of the last element and n is the total number of
elements, Takecare don't write (a1+an)/2 * n bec (a1+an)/2 may be double.
----------------------------------------------------------------------------------------------------------------------------------------
If i have an opertaion which is i pick m indexes of an array and decrease their value by one of the m indexes must be different i can do
that t times to turn all array values to zero okay i can say m*t is the number of decreases i can make so if the sum of all the elements
of the array is less than or equal to m*t so its possible to turn values to zero else its impossible but here there is one thing i am 
a fraid that may some element be decreased more than t times by choosing not distinct indexes at some of t times so i have to check first
whether the max of the array is less or equal to t if so it works you can use the rule else it is impossible.
----------------------------------------------------------------------------------------------------------------------------------------
If a ^ b = c and i have only a and c so b = a ^ c
----------------------------------------------------------------------------------------------------------------------------------------
If i am having array of numbers and want to get some subarray that the mean of it max, mean= sum of values/number of values.
observe that if i have 2 numbers a and b and a=b then mean 2*a/2 = a so if b is less than a then the mean will be smaller so the max
number of this array is the max mean.
----------------------------------------------------------------------------------------------------------------------------------------
Associative Property
A*(C*D) = (A*C)*D.
----------------------------------------------------------------------------------------------------------------------------------------
Most signficant is the most left and the less signficant is the most right.
----------------------------------------------------------------------------------------------------------------------------------------
-GCD of (n-1)*g and (n)*g is g, because let the gcd be x (n-1)*g = (n*g-g )/x okay (n*g)%x==0 but g%x have to be zero which means x=g.
----------------------------------------------------------------------------------------------------------------------------------------
- Sum of first N even numbers is N*(N+1).
- Sum of first N odd numbers is N^2.
----------------------------------------------------------------------------------------------------------------------------------------
If you want the sum of elements between L and R of an array, instead of getting F(L,R) which is the sum of elements of array
from L to R you can just reduce the problem and get the F(X) which is the sum from begining till X position of elements in
array if you have this function you can compute the sum from L to R by F[R]-F[L-1].
the same for multiplication functions.
----------------------------------------------------------------------------------------------------------------------------------------
Let x double number , 0<= x <=1 taking x^(inf) = 0, just like taking probability for inf number of turns will also equal to zero.
----------------------------------------------------------------------------------------------------------------------------------------
2^i = 2^(i-1) + 2^(i-1) = 2^(i-2) + 2^(i-2) + 2^(i-2) + 2^(i-2).
----------------------------------------------------------------------------------------------------------------------------------------
Multiplication Function:
is a property that some functions may have F[a*b]= f[a]*f[b], there is two kinds of multiplicative functions
1- completely multiplicative where a and b doesn't have to be co-prime.
2- conditional multimplacative where a and b have to be co-prime to be multiplicative.
In number theory usually a and b have to be co-prime to be multiplicative eg phi function.
----------------------------------------------------------------------------------------------------------------------------------------
Bi-jection (one to one correspondance):
a relation where i have 2 sets where each element from first set is mapped to exactly one element from the other set and vice versa.
----------------------------------------------------------------------------------------------------------------------------------------
Without lose of generality means that it is not only on a specific case of the problem it means it is on the general problem.
--------------------------------------------------------------------------------------------------------------------------------------
Odd number reverse the party but even number keeps the party without change
odd+odd=even, odd+even=odd.
----------------------------------------------------------------------------------------------------------------------------------------
cross a with d first, a/b = c/d --> a*d=b*c
----------------------------------------------------------------------------------------------------------------------------------------
gcd(a,b)=gcd(a,b%a).
----------------------------------------------------------------------------------------------------------------------------------------
ceil and floor differs with -ve values than from +ve.
----------------------------------------------------------------------------------------------------------------------------------------
4^(x) != 2^(2^x), so the base doesn't divide but only the power, 4^6 = 4^2^3  (2*3=6).
----------------------------------------------------------------------------------------------------------------------------------------
if you want to compare a*b*c to some number and a*b*c could overflow just take log for any base both sides bec log(a*b*c)=
log(a)+log(b)+log(c) which won't over flow and also take log the other side.
-log(a*b)=log(a)+log(b).
-log(a/b)=log(a)-log(b).
----------------------------------------------------------------------------------------------------------------------------------------
if i have a set of numbers A1,A2,A3 and i want get the sum of the multiplication of all the combinations of them 
which is A1A2+A1A3+A2A3+1 = (A1+1)*(A2+1)*(A3+1).
the same applied to the three elements treat '+' as or and '*' as and.
----------------------------------------------------------------------------------------------------------------------------------------
If you want to keep some range of bits as it is and cancel the rest you can do that by anding the mask with (1<<(range))-1.
----------------------------------------------------------------------------------------------------------------------------------------
if you have N blocks each contain 2 numbers each one block conmbine with two blocks and add to answer the 2 complete blocks
how many complete blocks you can form?
ans= N-(N-(ceil(N/3.0))
----------------------------------------------------------------------------------------------------------------------------------------
If you have a list of n bulbs each have two parameters ai and bi where each one toggle at ai and every bi seconds in time
then i want the max number of bulbs that will light at the same time then the max time they can meet at asumming that all starts
with zero is the product of all the bi then if they reach the same point then they will actually shifted by the a[i] then again to 
meet all at the same point it is enough to move till the product of all the a[i].
----------------------------------------------------------------------------------------------------------------------------------------
Mixed numbers: (integer division)((numerator mod denmenator)/denmenator).
----------------------------------------------------------------------------------------------------------------------------------------
if you want to compare two numbers a^b and c^d you can multiply log both sides but take care that the log for zero is undefined to 
handel this case alone.
code
        int a,b,c,d;
        cin>>a>>b>>c>>d;
        if(a==0){
            if(c==0) cout<<"Equal";
            else  cout<<"c^d > a^b";
        }
        else if(c==0){
            if(a==0) cout<<"Equal";
            else cout<<"a^b > c^d";
        }
        else{
        double f= b*log2(a);
        double s= d*log2(c);
        int ret=dcmp(f,s);
        if(ret==0)
            cout<<"Equal";
        else if(ret==1)
            cout<<"a^b > c^d";
        else
            cout<<"c^d > a^b";
        }
---------------------------------------------------------------------------------------------------------------------------------------
F1*a + F2*b = F3*gcd(a,b) , if F1 and F2 are non-negative integers only.
---------------------------------------------------------------------------------------------------------------------------------------
Monotonic Function: a function that whether increase only or decrease only.
---------------------------------------------------------------------------------------------------------------------------------------
Any polynomial of one variable when you equates it to zero means that the Xs that satisify the equation will be the intersection of
polynomial function with x-axis (which called the roots of the polynomial).
Example: y= x^3+2*x^2+x+3, 0=x^3+2*x^2+x+3, there is 3 roots which means 3 values of x that will locate at y equal to zero which
means it is the intersection of the graph with x-axis.
You can get the function from the roots it there is a polynomial with roots 3,4,6 then it is (x-3)*(x-4)*(x-5).
---------------------------------------------------------------------------------------------------------------------------------------
Any number could be expressed in base 2 (binary) but not any number can be expressed in other bases for example base 3 number 2 
couldn't be expressed in base 3.
---------------------------------------------------------------------------------------------------------------------------------------
if you are given some intervals [l,r] and if segment a ends at t and segment b starts at t then they don't intersect , and
we want to deal normally as if it intersects then subtract from the end of each segment one.
---------------------------------------------------------------------------------------------------------------------------------------
given polynomial p of degree n, its coffients are {a0,a1,..,an} if the gcd of all its coffients is one called primative polynomial,
if two primative polynomials multiplied the result is also a primative polynomial.
---------------------------------------------------------------------------------------------------------------------------------------
The solution to the problem may be randomized solution if the probabilty of failture is some like 2^(-20) you assume it to be zero 
so we can always find the  correct answer.
---------------------------------------------------------------------------------------------------------------------------------------
the first number the hundred th digit is zero is 1000 till 1000+99=1099
---------------------------------------------------------------------------------------------------------------------------------------
if you want to get the number of ways to choose n conscutive segments of length L such that non of them intersect from an array
of length H is :
Combination( H-(L-1)*n , n).
---------------------------------------------------------------------------------------------------------------------------------------
If number A is divisable by number B then it is divisable by each prime factor in B.
---------------------------------------------------------------------------------------------------------------------------------------
If you want to minimize the max(X,F-X) then the obtimal value for X is F/2 and if F/2 is positive and not divisbale by 2 the add one to the 
integer division of F/2.
---------------------------------------------------------------------------------------------------------------------------------------
If I have a range L and R and they can be positive or negative and i will add distance (d) to L but if it exceeds R then it will be with R
here if you did L=min(L+d,R) will work only if R is positive but if it is negative then L=max(L-d,R) so take care if the values are positive
or negative.
---------------------------------------------------------------------------------------------------------------------------------------
If you have a permutation of n numbers from 1 to n and you want to count the number of permuations such that 2 before 3 then 
note that for every combination of 2 choose n such that the two positions are 2 then 3 there is Factorial[n-2] ways to put the other 
elements and if we put 3 then 2 there is also Factorial[n-2] ways so the number of ways to let 2 before 3 equal to the number of ways to
put 3 before 2 and the sum of them equal to the total number of ways which is equal to Factorial[n].
---------------------------------------------------------------------------------------------------------------------------------------
if i have a set S the average of this set is (s1+s2+..+sn)/n, if i subtracted the average from each number of S the resulted sum of elements
will be zero, So formally segma(i=1 to i=n) (si-average_number) = zero. 
---------------------------------------------------------------------------------------------------------------------------------------
if you want to construct any simple quad of n sides and you have the n sides lengths only and you want to know whether i can construct 
this quad using sides with the given lengths in any order, to construct this quad every n-1 sides must be greater than the remaining side
if that's happened so the quad can be constructed.
---------------------------------------------------------------------------------------------------------------------------------------
If you have array of n elements and you can decrease or increase any element by one in one operation so that all the elements become the same what is the
value that every element should reach so that it minimize the total number of operations ? this number is not the average because here is an example
n=4 {1 8 9 9} the average is 6.75 so because the value should be integer so the obtimal value according to the average is 6 or 7 and the no
of operations to make all values equal to 6 is 13 and for 7 equal to 11 but if the value equal to 9 then the number of operations is 9 which is the
obtimal okay that's why ? because if the value is 6 or 7 if we go right one step to 8 for example the distance from 8,9,9 to value will decrease and from
1 will increase so the difference is -3+1 which is 2 so the overall answer will decrease by 2 so i should keep going till the number of elements on the right is
equal to or greater than the elements on the left.
So finally To know the proper value to make all numbers equal to in obtimal number of operations is:
1- sort all the elements increasingly then the value is the value of the middle element if the number
of elements is odd else try the two values at the center.
---------------------------------------------------------------------------------------------------------------------------------------
Let F(x) = x*x , if i have a number x then F(x) > F(x/2) + F(x/2) so dividing the number in to more partitions will decrease the total sum 
of F function for each part and to get the min sum the best thing is  to divide it into equal partions but there is a fact that 
( F(x)- (F(x/2)+F(x/2)) ) > ( F(x/2) - (F(x/4) + F(x/4)) ) .
---------------------------------------------------------------------------------------------------------------------------------------
the sum of digits of any multiple of 3 is also multiple of 3.
------------------------------------------------------------------------------------------------------------------------------------------
colouring the grid in the chess colours will divide the cells in to groups (white and black) where no two cells of different groups share
a side with the other.
------------------------------------------------------------------------------------------------------------------------------------------
if you want to get the number of pairs of segments that intersect each other where the segments can be in 4 types where [ this braket means 
included and this braket ( means excluded then if the bracket is ( at the start of the segment add some eps and if it is ) at the end subtract 
some eps and then check for each pair whether there is intersection or not.
------------------------------------------------------------------------------------------------------------------------------------------




















