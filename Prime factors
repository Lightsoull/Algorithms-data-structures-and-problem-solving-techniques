void primefact(int n)
{ 
   if(n<=1)
    return;
   
   for(int i=2;i*i<=n;i++)
   {
      while(n%i==0)
      {
         primef.pb(i);
         n/=i;
         }
         }
         
         if(n>1)
         primef.pb(n);
       }
    }
 }
 // works in squrt(n)
