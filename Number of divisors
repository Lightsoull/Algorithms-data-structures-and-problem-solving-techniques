void divs(int n)
{
    int sq=ceil(sqrt(n)),i;

    for(i=1;i<sq;i++)
    {
        if(n%i==0)
        {
            v[n].pb(i);
            v[n].pb(n/i);
        }
    }
    if(i*i==n)
        v[n].pb(i);
}
